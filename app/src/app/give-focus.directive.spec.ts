import { GiveFocusDirective } from './give-focus.directive';

describe('GiveFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new GiveFocusDirective();
    expect(directive).toBeTruthy();
  });
});
